#define _crt_secure_no_warnings
#include<stdio.h>
int main()
{
	int num1 = 0;
	int num2 = 0;
	scanf("%d%d", &num1, &num2);
	{
		int sum;
		sum = num1 + num2;
		printf("sum=%d\n", sum);
	}
	return 0;
 }
